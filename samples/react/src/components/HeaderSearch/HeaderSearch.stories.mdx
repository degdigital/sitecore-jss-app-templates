import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { default as HeaderSearch } from './index.js';
export const fields = {
    heading: {
        value: 'Sample Heading'
    }
};

<Meta title="Components/HeaderSearch" component={HeaderSearch} />

# HeaderSearch
This is the default example for the HeaderSearch component.

<Preview>
    <Story name="Example">
        <HeaderSearch fields={fields} />
    </Story>
</Preview>

## Props
<Props of={HeaderSearch} />

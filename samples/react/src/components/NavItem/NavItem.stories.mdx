import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { default as NavItem } from './index.js';
export const fields = {
    heading: {
        value: 'Sample Heading'
    }
};

<Meta title="Components/NavItem" component={NavItem} />

# NavItem
This is the default example for the NavItem component.

<Preview>
    <Story name="Example">
        <NavItem fields={fields} />
    </Story>
</Preview>

## Props
<Props of={NavItem} />

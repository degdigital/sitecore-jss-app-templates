import React from 'react';
// import PropTypes from 'prop-types';

const UtilityNav = (props) => (
  <div>
    <p>UtilityNav Component</p>
  </div>
);

// UtilityNav.propTypes = {
// };

UtilityNav.defaultProps = {
  fields: {
  }
};

export default UtilityNav;

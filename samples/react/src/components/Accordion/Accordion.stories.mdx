import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { default as Accordion } from './index.js';
export const fields = {
    headline: {
        value: 'Sample Heading'
    },
    description: {
        value: 'Sample Description'
    }
};

<Meta title="Components/Accordion" component={Accordion} />

# Accordion
This is the default example for the Accordion component.

<Preview>
    <Story name="Example">
        <Accordion fields={fields} />
    </Story>
</Preview>

## Props
<Props of={Accordion} />

import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { default as ContentPodGroup } from './index.js';
export const fields = {
    heading: {
        value: 'Sample Heading'
    }
};

<Meta title="Components/ContentPodGroup" component={ContentPodGroup} />

# ContentPodGroup
This is the default example for the ContentPodGroup component.

<Preview>
    <Story name="Example">
        <ContentPodGroup fields={fields} />
    </Story>
</Preview>

## Props
<Props of={ContentPodGroup} />

import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { default as SecondaryNavigation } from './index.js';
export const fields = {
    heading: {
        value: 'Sample Heading'
    }
};

<Meta title="Components/SecondaryNavigation" component={SecondaryNavigation} />

# SecondaryNavigation
This is the default example for the SecondaryNavigation component.

<Preview>
    <Story name="Example">
        <SecondaryNavigation fields={fields} />
    </Story>
</Preview>

## Props
<Props of={SecondaryNavigation} />

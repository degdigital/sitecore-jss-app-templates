import React from 'react';
// import PropTypes from 'prop-types';

const PrimaryNav = (props) => (
  <div>
    <p>PrimaryNav Component</p>
  </div>
);

PrimaryNav.propTypes = {
};

PrimaryNav.defaultProps = {
  fields: {
  }
};

export default PrimaryNav;
